'use strict';

// Configuring the Requests module
/*
angular.module('articles').run(['Menus',
	function(Menus) {
		// Set top bar menu items
		Menus.addMenuItem('topbar', 'Requests', 'requests', 'dropdown'); //, '/articles(/create)?'
		Menus.addSubMenuItem('topbar', 'requests', 'List Requests', 'articles');
		Menus.addSubMenuItem('topbar', 'requests', 'Manage Requests', 'articles/create', undefined, undefined);
	}
]);
*/